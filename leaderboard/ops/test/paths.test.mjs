import test from 'node:test';import assert from 'node:assert/strict';import os from 'node:os';import path from 'node:path';import {repoPath,root} from '../common.mjs';
test('file configuration uses repository root independently of npm working directory',()=>{const cwd=process.cwd();try{process.chdir(os.tmpdir());assert.equal(repoPath('./.priv-key'),path.join(root,'.priv-key'));assert.equal(repoPath('leaderboard/ops/deployments/test.json'),path.join(root,'leaderboard/ops/deployments/test.json'));assert.equal(repoPath('/tmp/demo.key'),'/tmp/demo.key');}finally{process.chdir(cwd);}});
