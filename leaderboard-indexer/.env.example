# Load explicitly, e.g. node --env-file=.env src/main.js. No keys are needed.
RPC_URL=
# Ethereum Sepolia was selected; address and block must come from real deployment.
CHAIN_ID=11155111
LEADERBOARD_ADDRESS=
DEPLOYMENT_BLOCK=
DB_PATH=data/leaderboard.sqlite
CONFIRMATIONS=2
BATCH_SIZE=100
POLL_MS=5000
HOST=127.0.0.1
PORT=8787
CORS_ORIGIN=*
# Optional JSON object keyed by canonical bytes32 chart hash:
# METADATA_PATH=charts.json
