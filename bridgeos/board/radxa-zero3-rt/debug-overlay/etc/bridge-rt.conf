# Debug profile only: save startup errors in tmpfs for the FAT diagnostic snapshot.
BRIDGE_DIAG_LOG=/run/bridge-startup.log
# Explicitly insecure protocol test backend: maximum 65 events from dev SRS.
OSUMANIA_SIGNER_BACKEND=dev-insecure
OSUMANIA_SRS=/usr/share/osumania/srs-g1-be.bin
