import test from 'node:test';import assert from 'node:assert/strict';import {localAccess} from '../local-access.mjs';
test('unauthenticated bridge refuses public binds and remote origins',()=>{for(const host of ['0.0.0.0','::','192.168.1.2','demo.example'])assert.throws(()=>localAccess({host}));assert.throws(()=>localAccess({allowedOrigins:['https://demo.example']}));assert.equal(localAccess({host:'localhost'}).host,'127.0.0.1');});
test('blocks DNS rebinding, nonlocal peers and cross-origin requests',()=>{const access=localAccess({port:8788,allowedOrigins:['http://localhost:3015']});const req={method:'POST',headers:{host:'127.0.0.1:8788',origin:'http://localhost:3015','content-type':'application/json'},socket:{remoteAddress:'127.0.0.1'}};assert.equal(access.check(req),null);assert.equal(access.check({...req,headers:{...req.headers,host:'evil.example:8788'}}),'Host not allowed');assert.equal(access.check({...req,headers:{...req.headers,origin:'https://evil.example'}}),'Origin not allowed');assert.equal(access.check({...req,socket:{remoteAddress:'192.168.1.3'}}),'Loopback clients only');assert.equal(access.check({...req,headers:{...req.headers,'content-type':'text/plain'}}),'JSON content type required');assert.equal(access.check({...req,headers:{...req.headers,origin:undefined}}),null);});
